# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8080

# Authentication Mode
# Set to "cookie" to use HTTPOnly cookie authentication (production)
# Set to "bearer" to use traditional Bearer token authentication (development)
NEXT_PUBLIC_AUTH_MODE=bearer

# Feature Flags
NEXT_PUBLIC_USE_COOKIE_AUTH=false
